apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: sandboxes.sandbox.fast.io
spec:
  group: sandbox.fast.io
  names:
    kind: Sandbox
    listKind: SandboxList
    plural: sandboxes
    singular: sandbox
  scope: Namespaced
  versions:
  - name: v1alpha1
    served: true
    storage: true
    schema:
      openAPIV3Schema:
        type: object
        properties:
          spec:
            type: object
            properties:
              image: {type: string}
              command: {type: array, items: {type: string}}
              args: {type: array, items: {type: string}}
              poolRef: {type: string}
              expireTime: {type: string, format: date-time}
              exposedPorts: {type: array, items: {type: integer}}
              failurePolicy: {type: string, enum: ["Manual", "AutoRecreate"]}
              recoveryTimeoutSeconds: {type: integer}
              resetRevision: {type: string, format: date-time}
          status:
            type: object
            properties:
              phase: {type: string}
              assignedPod: {type: string}
              nodeName: {type: string}
              sandboxID: {type: string}
              endpoints: {type: array, items: {type: string}}
              acceptedResetRevision: {type: string, format: date-time}
              conditions:
                type: array
                items:
                  type: object
                  properties:
                    type: {type: string}
                    status: {type: string}
                    reason: {type: string}
                    message: {type: string}
                    lastTransitionTime: {type: string, format: date-time}
    subresources:
      status: {}
